# the-arcane-query

**Lessons**
- Defer in the head, makes it so that the script is downloaded in parallel while HTML is parsed, and executes after the HTML is fully parsed. I read it's supposedly the modern alternative and also makes it cleaner.